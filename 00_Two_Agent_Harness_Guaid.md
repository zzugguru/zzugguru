# 2에이전트 하네스 아키텍처와 Codex 운영 가이드

이 문서는 하네스 개발이 처음인 팀원이 Codex에서 **메인 구현 에이전트 1개와 검증 서브에이전트 1개**를 사용해 기능 하나를 안전하게 완성하는 방법을 설명합니다. 이 저장소에는 공식 Codex 탐색 규칙에 맞춘 `AGENTS.md`와 팀 공유 스킬 `.agents/skills/two-agent-harness/`가 실제로 설정되어 있습니다.

여기서 하네스는 게임 테스트 코드만 뜻하지 않습니다. 사람이 매번 긴 지시를 반복하지 않아도 두 에이전트가 같은 절차와 완료 기준에 따라 작업하도록 만드는 **작업 운영 체계**를 뜻합니다.

---

## 1. 가장 먼저 이해할 구조

개발자 한 명이 하나의 기능을 맡고, Codex 안에서 두 에이전트를 운영합니다.

```text
개발자
  │
  ├─ 요구사항 결정과 최종 승인
  │
  ▼
에이전트 A: 구현자(Main Agent)
  │
  ├─ 요구사항 분석
  ├─ 클라이언트와 서버 로직 구현
  ├─ 테스트 작성 및 실행
  └─ 변경 사항 정리
  │
  ▼
에이전트 B: 검증자(Reviewer Subagent)
  │
  ├─ 요구사항 충족 여부 검토
  ├─ 버그와 회귀 위험 탐색
  ├─ 누락된 테스트 확인
  └─ 근거가 있는 지적만 보고
  │
  ▼
에이전트 A가 수정 → 에이전트 B가 재검증 → 개발자가 승인
```

이 문서에서 **2에이전트**는 메인 에이전트와 서브에이전트를 합친 수입니다. 메인 에이전트가 서브에이전트 두 개를 생성하면 총 3에이전트가 되므로 이 하네스의 범위를 벗어납니다.

## 2. 왜 역할을 분리하는가?

하나의 에이전트가 구현과 자기 검토를 모두 수행하면 자신이 세운 가정을 그대로 유지하기 쉽습니다. 검증 에이전트에는 코드를 작성한 맥락보다 요구사항과 변경 결과를 중심으로 제공해 다른 관점에서 확인하게 합니다.

역할 분리의 목적은 사람을 흉내 내는 것이 아니라 다음과 같은 검증 단계를 반복 가능하게 만드는 것입니다.

- 구현 결과가 요구사항과 일치하는지 확인
- 정상 흐름뿐 아니라 실패와 경계 조건 확인
- 클라이언트와 서버의 계약 불일치 탐지
- 테스트가 구현의 실수를 실제로 잡을 수 있는지 확인
- 변경 범위 밖의 코드가 우연히 바뀌지 않았는지 확인

서브에이전트는 별도의 모델·도구 작업을 수행하므로 단일 에이전트보다 토큰을 더 사용합니다. 작은 오탈자나 한 줄 수정에는 사용하지 않고, 클라이언트와 서버가 연결되는 기능이나 회귀 위험이 있는 작업에 사용하는 것이 효율적입니다.

## 3. 두 에이전트의 책임

### 에이전트 A: 구현자

구현자는 기능을 실행 가능한 상태까지 완성합니다.

- 관련 코드와 문서를 먼저 조사
- 요구사항과 완료 조건 정리
- 필요한 경우 구현 계획 작성
- 기능 디렉터리 안에서 클라이언트, 서버, 공유 타입 구현
- 단위 테스트와 기능 재현 시나리오 작성
- 타입 검사, 테스트, 빌드 실행
- 검증자가 읽을 수 있도록 변경 범위와 남은 위험 요약
- 검증 결과 중 타당한 항목 수정

구현자가 해서는 안 되는 일:

- 요구사항에 없는 대규모 리팩터링
- 실패한 테스트를 이유 없이 삭제하거나 약화
- 검증 전에 완료되었다고 선언
- 검증자의 독립적인 판단을 유도하는 결론 강요

### 에이전트 B: 검증자

검증자는 기본적으로 **읽기와 검토만 수행**합니다. 직접 수정하지 않고, 발견 내용과 재현 근거를 메인 에이전트에 반환합니다.

- 요구사항과 완료 조건을 기준으로 변경 diff 검토
- 버그, 회귀, 보안, 데이터 무결성 문제 확인
- 클라이언트 요청 타입과 서버 처리의 일치 여부 확인
- 성공, 실패, 경계 조건의 테스트 범위 확인
- 가능하면 관련 테스트와 검사 명령 실행
- 발견 항목을 심각도와 파일 위치, 재현 방법과 함께 보고
- 문제가 없다면 무엇을 확인했는지와 남은 위험 보고

검증자가 해서는 안 되는 일:

- 구현자와 동시에 같은 파일 수정
- 취향만 다른 스타일을 결함으로 보고
- 범위 밖 리팩터링을 필수 수정으로 요구
- 확인하지 않은 가능성을 확정된 버그로 표현
- 단순히 “문제 없음”이라고만 답하고 검증 근거 생략

## 4. 공식 Codex 하네스 디렉터리

```text
.
├── AGENTS.md
├── BRIEF.md
├── .agents/
│   └── skills/
│       └── two-agent-harness/
│           ├── SKILL.md
│           └── agents/
│               └── openai.yaml
└── 00_Two_Agent_Harness_Guaid.md
```

- `AGENTS.md`: 이 저장소에서 수행하는 Codex 작업에 자동 적용되는 지속 지침
- `BRIEF.md`: 현재 작업의 개발자 요청, 에이전트 질문, 결정과 결과를 공유하는 단일 창구
- `SKILL.md`: 기능 구현 시 사용하는 2에이전트 절차와 검증 규칙
- `agents/openai.yaml`: Codex UI에 표시할 스킬 이름, 설명, 기본 호출 프롬프트
- `00_Two_Agent_Harness_Guaid.md`: 개발자가 읽는 온보딩 및 운영 설명서

검증 서브에이전트는 기본적으로 `gpt-5.6-terra`와 `high` reasoning effort를 사용합니다. 인증·권한·보안·결제·데이터 마이그레이션·동시성·데이터 무결성, 대규모 아키텍처 변경, 복합 기능 버그처럼 위험도가 높은 작업은 `gpt-5.6-sol`과 `high` reasoning effort로 승격합니다.

스킬은 `$two-agent-harness`로 명시적으로 호출할 수 있습니다. 또한 이 저장소의 `AGENTS.md`는 동작을 변경하는 기능 개발에 이 스킬을 사용하도록 지시합니다.

`AGENTS.md`의 탐색은 Codex 실행을 시작할 때 한 번 이루어집니다. 이 파일을 새로 추가하거나 변경한 직후에는 **새 Codex 대화나 세션을 시작해 적용**합니다. 새 대화에서 다음처럼 확인할 수 있습니다.

```text
현재 저장소에서 읽은 프로젝트 지침과 사용 가능한 two-agent-harness 스킬을 요약해줘.
```

## 5. 기능 코드 디렉터리

기능 코드는 현재 프로젝트의 기능 중심 구조를 따릅니다.

```text
src/features/<기능명>/
├── client/                 # UI, 입력, 클라이언트 상태
├── server/                 # API, 검증, 저장 로직
├── shared/                 # 요청/응답 타입과 기능 공통 규칙
└── tests/                  # 테스트, fixture, mock, 재현 시나리오
```

현재 작업의 명세는 루트 `BRIEF.md`에 기록합니다. 작업마다 새 파일을 만들지 않고 같은 파일을 재사용합니다.

```text
BRIEF.md
├── Developer Request       # 목표, 범위, 제약, 완료 조건
├── Agent Understanding     # 메인 에이전트가 이해한 작업
├── Agent Questions         # 판단이 필요한 질문
├── Developer Decisions     # 개발자의 추가 결정
├── Agent Result            # 구현과 검증 결과
├── Developer Final Check   # 개발자 최종 확인
└── Completed Work Log      # 완료 작업 한 줄 기록
```

개발자 소유 영역은 에이전트가 명시적인 요청 없이 변경하지 않습니다. 작업이 끝난 뒤 요약을 `Completed Work Log`에 남기고 현재 영역을 다음 작업에 재사용합니다. 이전 상세 내용은 Git 이력에 남습니다.

### `AGENTS.md`에 적합한 내용

`AGENTS.md`는 Codex가 저장소에서 자동으로 참고하는 지속적인 팀 지침입니다.

- 저장소 디렉터리 설명
- 설치, 개발 서버, 테스트, 빌드 명령
- 기능 단위 디렉터리 규칙
- 수정하면 안 되는 파일 또는 보안 규칙
- 완료 조건과 리뷰 기준
- “구현 후 검증 서브에이전트 한 개를 사용한다”는 운영 규칙

특정 기능의 목표, 범위와 완료 조건은 `AGENTS.md`에 넣지 않고 `BRIEF.md`에 기록합니다.

## 6. 시작 전 준비

### 6.1 저장소 상태 확인

기능 작업을 시작하기 전에 다음을 확인합니다.

```bash
git status
npm install
npm run typecheck
npm run test
```

기존 테스트가 실패한다면 작업 시작 전에 팀에 공유합니다. 기능 작업 때문에 생긴 실패와 원래 존재하던 실패를 구분할 수 있어야 합니다.

### 6.2 기능을 작게 자르기

좋지 않은 작업 단위:

```text
인벤토리 전체를 구현한다.
```

권장 작업 단위:

```text
보유 아이템 목록을 조회한다.
아이템 하나를 사용한다.
아이템을 폐기한다.
```

각 작업은 클라이언트와 서버를 포함하더라도 하나의 사용자 결과로 설명할 수 있어야 합니다.

### 6.3 완료 조건 작성

구현을 요청하기 전에 최소한 다음 네 항목을 작성합니다.

```markdown
# 아이템 사용

## 목표
플레이어가 인벤토리에서 회복 아이템을 사용할 수 있다.

## 범위
- 아이템 사용 버튼
- 사용 요청 API
- 서버의 소유권 및 사용 가능 여부 검증
- 성공 후 수량과 체력 갱신

## 제외 범위
- 아이템 구매
- 장비 아이템
- 사용 애니메이션

## 완료 조건
- 보유한 회복 아이템을 사용하면 수량이 1 감소한다.
- 체력이 최대치 이상으로 증가하지 않는다.
- 보유하지 않은 아이템 요청은 서버가 거부한다.
- 중복 요청으로 아이템이 두 번 차감되지 않는다.
- 타입 검사, 테스트, 빌드가 통과한다.
```

## 7. Codex에서 실행하는 순서

### 1단계: 기능별 대화 시작

하나의 Codex 대화에는 하나의 일관된 결과만 맡깁니다. 이전 기능의 대화를 계속 재사용하기보다 새 기능은 새 대화에서 시작합니다.

Codex 앱에서 프로젝트를 열고 기능 브랜치 또는 별도 worktree에서 대화를 시작합니다. 동시에 진행되는 다른 작업이 같은 파일을 수정할 가능성이 있다면 worktree를 사용하는 것이 안전합니다.

### 2단계: 구현자에게 작업 요청

먼저 `BRIEF.md`의 `Developer Request`에 목표, 컨텍스트, 제약, 완료 조건을 작성하고 상태를 `READY`로 바꿉니다. Codex 프롬프트는 하네스 실행만 요청합니다.

```text
$two-agent-harness를 사용해서 BRIEF.md의 현재 작업을 구현해줘.
구현 전에 Developer Request를 읽고 Agent Understanding을 작성해.
판단이 필요한 부분은 Agent Questions에 기록하고 나에게 확인해줘.
```

복잡하거나 요구사항이 아직 모호하면 Codex의 Plan 모드에서 먼저 범위와 계획을 확정하고 구현을 시작합니다.

### 3단계: 구현자가 코드 작성 및 자체 검사

메인 에이전트는 관련 코드를 읽고 구현한 뒤 다음을 실행해야 합니다.

```bash
npm run typecheck
npm run test
npm run build
git diff --check
```

프로젝트에 lint나 E2E 명령이 추가되면 `AGENTS.md`의 완료 조건에도 함께 기록합니다.

### 4단계: 검증 서브에이전트 실행

Codex 앱 또는 IDE에서는 서브에이전트 활동 패널에서 진행 상황을 볼 수 있습니다. CLI에서는 `/agent`로 에이전트 스레드를 확인하고 전환할 수 있습니다.

서브에이전트에는 아래처럼 범위가 명확한 읽기 전용 작업을 전달합니다.

```text
검증자로서 현재 작업 트리의 아이템 사용 기능을 독립적으로 검토해줘.

검토 기준:
1. `BRIEF.md`의 Developer Request와 Developer Decisions를 모두 충족하는가?
2. 클라이언트 요청과 서버의 타입·검증 규칙이 일치하는가?
3. 중복 요청, 잘못된 ID, 아이템 미보유, 최대 체력 경계를 처리하는가?
4. 테스트가 구현 세부사항이 아닌 사용자 동작과 서버 규칙을 검증하는가?
5. 범위 밖 변경이나 기존 기능 회귀 위험이 있는가?

규칙:
- 파일을 수정하지 않는다.
- 가능하면 관련 검사를 직접 실행한다.
- 발견 사항은 심각도순으로 파일과 위치, 재현 방법, 영향, 권장 수정 방향을 보고한다.
- 발견 사항이 없으면 확인한 항목과 아직 검증하지 못한 위험을 보고한다.
```

두 에이전트가 같은 작업 디렉터리를 공유한다면 **검증자가 활동하는 동안 구현자는 파일을 수정하지 않습니다**. 검증 기준이 움직이는 것을 막기 위한 규칙입니다.

### 5단계: 검증 결과 분류

개발자와 구현자는 검증 결과를 다음과 같이 분류합니다.

| 분류 | 처리 |
|---|---|
| 반드시 수정 | 요구사항 위반, 런타임 오류, 데이터 손상, 보안 문제 |
| 이번 작업에서 수정 | 작은 회귀 위험, 누락된 핵심 테스트 |
| 후속 작업 | 범위 밖 개선, 성능 최적화, 공통화 후보 |
| 기각 | 근거가 없거나 요구사항과 충돌하는 제안 |

검증자의 모든 제안을 자동으로 수용하지 않습니다. 구현자에게 각 항목의 근거를 확인하게 하고, 기능 목표와 관련된 항목만 반영합니다.

### 6단계: 구현자가 수정

구현자는 검증 보고서의 타당한 항목만 최소 범위로 수정하고 검사를 다시 실행합니다. 수정 과정에서 요구사항이나 API 계약을 바꿔야 한다면 먼저 개발자에게 확인하고, 확정된 내용은 `BRIEF.md`의 `Developer Decisions`에 개발자가 기록합니다.

### 7단계: 같은 검증자에게 재검증 요청

새 검증자를 추가하지 않고 기존 검증 서브에이전트에게 후속 검토를 요청합니다.

```text
지적한 항목을 수정했다. 같은 완료 조건을 기준으로 변경 사항을 다시 확인해줘.
이전 발견 사항이 해결됐는지와 수정으로 새 회귀가 생기지 않았는지 검토하고,
파일은 수정하지 않은 채 결과만 보고해줘.
```

같은 검증자를 재사용하면 최초 발견 사항과 수정 내용을 비교할 수 있고, 총 에이전트 수도 두 개로 유지됩니다.

### 8단계: 개발자 최종 승인

개발자는 다음을 직접 확인합니다.

- 기능을 브라우저에서 실행해 핵심 흐름 확인
- Codex 변경 diff 확인
- 검증 보고서와 미해결 위험 확인
- 불필요한 파일과 비밀정보가 포함되지 않았는지 확인
- 커밋과 Pull Request 범위가 기능 하나에 집중하는지 확인

Codex 앱, CLI, IDE에서 `/review`를 사용하면 현재 변경, 특정 커밋 또는 기준 브랜치 대비 diff를 전용 리뷰 흐름으로 추가 확인할 수 있습니다. `/review`는 변경 내용을 자동 수정하지 않고 우선순위가 있는 발견 사항을 제공합니다.

## 8. 완료 판정표

아래 항목이 모두 충족되어야 기능을 완료로 봅니다.

```text
[ ] 기능 목표와 제외 범위가 기록되어 있다.
[ ] 클라이언트와 서버의 계약이 일치한다.
[ ] 서버가 권한, 소유권, 입력값을 검증한다.
[ ] 정상, 실패, 경계 조건 테스트가 존재한다.
[ ] 기능 테스트 시나리오로 핵심 상황을 재현할 수 있다.
[ ] typecheck, test, build가 통과한다.
[ ] 검증 에이전트의 독립 검토가 끝났다.
[ ] 필수 발견 사항을 수정하고 재검증했다.
[ ] 개발자가 diff와 실제 동작을 확인했다.
```

## 9. 에이전트 간 전달 형식

구현자가 검증자에게 넘기는 요약은 다음 형식을 권장합니다.

```markdown
## 구현 목표

## 변경 파일

## 주요 설계 결정

## 실행한 검사와 결과

## 확인이 필요한 위험

## 의도적으로 제외한 범위
```

검증자의 보고 형식은 다음을 권장합니다.

```markdown
## 발견 사항

### [높음/중간/낮음] 제목
- 위치:
- 재현 방법 또는 근거:
- 영향:
- 권장 수정 방향:

## 실행한 검사

## 확인하지 못한 영역

## 최종 판정
- 승인 / 수정 후 재검증 필요
```

발견 사항이 없을 때도 `확인하지 못한 영역`은 반드시 적습니다. 검증 완료와 모든 위험이 사라졌다는 의미는 다릅니다.

## 10. Git과 worktree 운영

한 개발자의 구현자와 검증자는 같은 기능을 순차적으로 다루므로 같은 feature branch를 사용해도 됩니다. 검증자는 읽기 전용으로 운용합니다.

서로 다른 개발자가 기능을 동시에 개발할 때는 각자 별도 브랜치를 사용합니다.

```text
feature/player-dash
feature/inventory-use-item
feature/ranking-list
```

Codex 앱의 worktree는 저장소의 별도 체크아웃을 만들어 다른 대화의 변경과 충돌하지 않게 합니다. 다만 Git은 같은 브랜치를 둘 이상의 worktree에서 동시에 checkout하는 것을 허용하지 않습니다. worktree마다 별도 브랜치를 사용하거나 앱의 Handoff 기능으로 작업을 Local로 옮깁니다.

두 에이전트가 같은 파일을 동시에 수정하게 만들지 마세요. 공식 Codex 가이드도 병렬 에이전트는 탐색, 테스트, 분류, 요약 같은 읽기 중심 작업부터 시작하고, 병렬 쓰기 작업은 충돌과 조정 비용에 주의하도록 권장합니다.

## 11. 자주 발생하는 실패와 대응

### 검증자가 코드를 직접 수정함

검증 프롬프트에 `파일을 수정하지 않는다`를 명시합니다. 이미 수정했다면 변경을 무조건 삭제하지 말고 diff를 확인해 구현 변경과 분리합니다.

### 두 에이전트가 같은 결론만 반복함

검증자에게 구현자의 상세 추론 대신 요구사항, diff, 테스트 결과를 중심으로 제공합니다. 특정 결론을 확인하라고 유도하지 말고 반례와 실패 조건을 찾게 합니다.

### 검증 결과가 지나치게 많음

결함, 회귀, 요구사항 누락만 보고하도록 범위를 좁힙니다. 스타일 선호와 범위 밖 리팩터링은 제외하도록 명시합니다.

### 기능 범위가 계속 커짐

`BRIEF.md`의 Excluded Scope에 제외 범위를 기록하고 새 요구사항은 후속 작업으로 분리합니다. 공통화도 현재 기능에 필수적이지 않으면 별도 리팩터링 작업으로 남깁니다.

### 테스트가 통과하지만 실제 화면이 깨짐

자동 테스트와 별개로 브라우저에서 확인할 하네스 시나리오를 만듭니다. 레이아웃, 입력감, 애니메이션처럼 시각·상호작용 검증이 필요한 항목은 사람이 최종 확인합니다.

### 대화가 너무 길어짐

기능 하나당 대화 하나를 유지합니다. 같은 문제의 연속이라면 기존 대화를 유지하고, 작업이 실제로 갈라질 때만 새 대화나 fork를 사용합니다. 긴 대화에서는 `/compact`로 이전 내용을 요약할 수 있습니다.

## 12. 처음 적용할 때의 최소 운영안

처음부터 복잡한 자동화나 커스텀 에이전트 설정을 만들 필요는 없습니다. 첫 2~3개 기능은 다음 절차만 지킵니다.

1. 기능의 목표, 범위, 제외 범위, 완료 조건을 작성한다.
2. 메인 에이전트에게 구현과 테스트를 맡긴다.
3. 구현이 끝난 뒤 검증 서브에이전트 한 개를 요청한다.
4. 검증자는 읽기 전용으로 diff와 테스트를 확인한다.
5. 메인 에이전트가 수정하고 같은 검증자가 재검토한다.
6. 개발자가 실제 동작과 diff를 승인한다.
7. 반복되는 실수만 `AGENTS.md`에 규칙으로 추가한다.

이 흐름이 안정적으로 반복된 다음에만 역할별 커스텀 에이전트, 팀 스킬, 자동화 도입을 검토합니다.

## 13. 팀 회고에서 확인할 지표

기능 완료 후 다음 질문에 짧게 답합니다.

- 검증 에이전트가 실제 결함을 발견했는가?
- 사람이 발견한 문제 중 에이전트가 놓친 것은 무엇인가?
- 프롬프트나 완료 조건이 모호했던 부분은 무엇인가?
- 두 에이전트 때문에 늘어난 시간과 토큰이 품질 향상에 비해 적절했는가?
- 다음 기능에서도 반복될 규칙만 `AGENTS.md`에 추가했는가?

검증 에이전트가 계속 가치 없는 지적만 한다면 에이전트를 늘리기보다 완료 조건과 리뷰 체크리스트를 먼저 개선합니다.

## 14. 공식 Codex 참고 자료

- [Codex Best practices](https://learn.chatgpt.com/guides/best-practices)
- [Codex Subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents)
- [Codex AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
- [Codex Code review](https://learn.chatgpt.com/docs/code-review)
- [Codex Worktrees](https://learn.chatgpt.com/docs/environments/git-worktrees)

이 문서의 Codex 운영 방법은 2026-08-06에 확인한 공식 Codex 매뉴얼을 기준으로 작성했습니다. 제품 UI나 명령은 버전에 따라 달라질 수 있으므로 팀 환경에서 보이지 않는 기능이 있다면 현재 설치 버전과 계정 제공 범위를 먼저 확인합니다.
