# Current Asset Work Brief

이 파일은 에셋 작업 전용 요청, 질문, 결정과 결과를 공유하는 단일 창구입니다. 로직 작업의 `BRIEF.md`와 독립적으로 관리합니다.

## Status

`READY_FOR_DEVELOPER_CHECK`

상태 흐름: `DRAFT` → `READY` → `IN_PROGRESS` → `REVIEW` → `ACCEPTED`

## Developer Request

> 개발자 소유 영역입니다. 에이전트는 명시적인 요청 없이 수정하거나 삭제하지 않습니다.

### Task

Chapter 3 기록 보관소 배경 생성 및 오브젝트 통합

### Goal

현재 도형으로만 표시되는 기록 보관소에 전용 탑다운 픽셀 아트 배경을 추가하고, 기존 기록 오브젝트와 출입문을 공간 구조에 자연스럽게 배치해 후일담 탐색 장면의 장소성과 가독성을 완성한다.

### Context

- 관련 기능: `src/features/chapter3/client/MemoryReconstructionGame.ts`, `epilogueBackground.ts`, `shared/epilogueLogic.ts`
- `archive` 단계는 전용 배경 없이 단색 바닥·격자·도형 fallback을 사용한다.
- `interaction-objects.png`에는 `archive-photo`, `archive-voice`, `archive-tool`, `archive-star`, `archive-table`, `archive-door` 프레임이 이미 존재한다.

### Included Scope

- 960×540 캔버스용 기록 보관소 탑다운 픽셀 아트 배경 PNG 생성
- 배경 로딩·선택·nearest-neighbor 렌더링과 로딩 실패 fallback 통합
- 기존 archive 기록 4종, 기록 테이블과 출입문 에셋을 새 배경 구조에 맞춰 적용
- 기존 기록 배치 좌표·이동 범위를 기준으로 충돌·도달성·화면 경계 검증 및 필요한 최소 좌표 조정
- 배경·오브젝트 래스터 메타데이터, import, draw geometry와 모든 상호작용 도달성 회귀 테스트

### Excluded Scope

- `interaction-objects.png` 프레임 이미지 자체 변경
- archive 이외 기억방·개인실·몽타주 배경 변경
- 대사, 기록 순서, 선택 결과와 장면 흐름 변경
- Chapter 1·2 및 logic lane 작업 변경

### Constraints

- `AGENTS.md`, `DESIGN.md`, `$two-agent-harness`, `imagegen` 절차를 따른다.
- 배경은 960×540 RGB 또는 RGBA PNG이며 기존 어두운 인디고·목재 계열 탑다운 픽셀 아트와 조명 방향을 따른다.
- 기록 오브젝트와 출입문은 배경에 직접 그리지 않고 별도 스프라이트로 유일하게 표시한다.
- 이동 가능 바닥, 입구, 모든 기록 지점은 영수의 64×80 가시 실루엣과 26×26 이동 기준을 고려해 화면 안에서 도달 가능해야 한다.
- 모든 생성·수정·재검증 반복은 작업당 최대 2회이며, 이후 미완료 시 보고하고 진행 여부를 확인한다.

### Done When

- [ ] 기록 보관소 전용 960×540 배경이 archive 단계에 표시된다.
- [ ] 기록 4종, 기록 테이블과 출입문이 배경 구조와 겹침 없이 자연스럽게 표시된다.
- [ ] 영수의 spawn·이동·모든 기록 배치 상호작용이 화면 안에서 도달 가능하다.
- [ ] 배경 로딩 실패 시 기존 fallback과 오브젝트가 유지된다.
- [ ] 필수 검사와 Terra 독립 리뷰가 완료된다.

## Agent Understanding

> 메인 에이전트 소유 영역입니다. 구현 전에 이해한 목표, 범위와 완료 조건을 기록합니다.

`BRIEF_ASSET.md` 자산 lane에서 960×540 기록 보관소 탑다운 픽셀 아트 배경을 생성하고 archive 단계에 통합한다. 기존 오브젝트 시트의 기록 4종·테이블·문은 배경에 중복해 그리지 않고 현재 좌표와 이동 범위를 우선해 배치한다. 배경 실패 fallback, 64×80 영수 실루엣, 모든 상호작용 도달성과 래스터·draw geometry를 검증한다.

## Agent Questions

> 메인 에이전트 소유 영역입니다. 개발자의 판단이 필요한 질문과 확인 상태를 기록합니다.

해결됨. 2026-08-10 사용자가 권장 범위대로 기록 보관소 배경과 archive 오브젝트 통합을 진행하도록 확인했다.

## Developer Decisions

> 개발자 소유 영역입니다. 에이전트는 명시적인 요청 없이 수정하거나 삭제하지 않습니다.

- 2026-08-09: 사용자가 제공한 노년 경비원 캐릭터 이미지를 영수의 얼굴, 헤어스타일, 체형과 연령감의 주 참조로 사용한다. 손전등·무전기·커피·가족사진 소품은 스프라이트에 포함하지 않는다.
- 2026-08-09: 참고 이미지의 마른 노년 체형을 유지하기 위해 초기의 실루엣 약 26px 폭보다 시각적 일치를 우선한다. 최종 폭은 앞·뒤 18px, 좌·우 14px로 하되 960×540 맵 합성으로 가독성을 검증한다.
- 2026-08-09: 확정한 영수 스프라이트는 여러 챕터에서 사용할 공통 에셋으로 `src/assets/`에 보관한다.
- 2026-08-10: 원본 64×80 프레임은 유지하고 Chapter 3 게임 표시 크기를 48×60으로 확대한다. 충돌 영역은 기존 26×26을 유지한다.
- 2026-08-10: 재구성 가족과 작별한 뒤 누나와 대화하는 장면의 배경은 작고 소박한 우주선 내 영수 개인실로 설정한다.
- 2026-08-10: 영수 스프라이트는 Chapter 3에서 원본 프레임과 동일한 64×80 크기로 표시한다. 충돌·상호작용 크기는 26×26을 유지한다.
- 2026-08-10: Chapter 2 배경은 지하 경비실, 외계 어린 시절 방, 우주선 홀로그램 기록실, 우주선 전망실의 네 종류로 구성한다.
- 2026-08-10: 검증 에이전트는 `gpt-5.6-terra`로 고정하고, 모든 반복 작업은 최대 2회 후 사용자에게 계속 진행할지 확인한다.
- 2026-08-10: Chapter 2·3의 오브젝트 충돌 영역은 중심을 유지한 채 가로·세로를 90%로 축소한다.
- 2026-08-10: Chapter 2·3 상호작용 오브젝트는 배경과 같은 탑다운 픽셀 아트 에셋으로 교체하고, 작은 오브젝트는 투명 스프라이트 시트로 관리한다.
- 2026-08-10: 재구성 가족은 우주복·제복이 아닌 평상복을 입는다. 큰아들은 12–14세, 작은딸은 7–9세로 처음 생성안보다 어리게 표현한다.

## Agent Result

> 메인 에이전트 소유 영역입니다. 구현 결과, 검사 결과, 리뷰 대응과 남은 위험을 기록합니다.

- `chapter03-archive.png` 960×540 RGB 탑다운 픽셀 아트 배경을 생성해 archive·archive-complete 단계에 통합했다. 배경은 다섯 개의 빈 보존 패드, 오른쪽 입구 홈, 연결된 중앙 바닥을 포함하며 상호작용 오브젝트 자체는 배경에 중복하지 않는다.
- 기존 오브젝트 시트의 가족사진 `(230,170)`, 아내의 목소리 `(307,375)`, 큰아들 도구 `(463,170)`, 작은딸 별 좌표 `(632,375)`, 마지막 식탁 `(712,170)`, 내부 문 `(850,270)`을 각 패드·입구에 적용했다. 기록 순서·대사·상태 로직은 유지했다.
- archive 배경은 정적 import되어 960×540→960×540, smoothing 비활성화로 렌더링된다. 로딩 실패 시 기존 단색 배경과 기록·문 fallback 도형이 유지된다.
- 영수 실제 가시 실루엣을 반영해 열린 바닥을 `(115,120)–(825,451)`, 이동 위치를 `x=120..794, y=162..425`, spawn을 `(140,270)`으로 정의했다. 모든 기록과 문은 BFS 검증에서 안전 바닥 경로로 도달 가능하다.
- 6개 archive 프레임의 실제 알파 경계를 PNG에서 읽어 렌더링 배율과 패드·입구 포함 관계를 검증한다. 배경은 960×540 8-bit RGB 및 SHA-256 `0a18c1231e6c579f5c95739fdf368c6c198a63d984fee68ae248395d99cb218d`로 고정했다.
- Terra 독립 리뷰의 벽·선반 통과, 초기 spawn 겹침, 실제 알파·연결 경로 테스트 누락과 하단 선반 4px 겹침 지적을 두 번의 수정 반복으로 해결했다. 같은 리뷰어의 최종 재검증에서 새 차단 회귀 없음이 확인됐다.
- 최종 검사: `npm run typecheck`, `npm run test`(179/179), `npm run build`, `git diff --check` 모두 통과했다.
- 남은 위험: 브라우저 런타임이 없어 실제 조작 화면은 수동 검증하지 못했다. 대신 실제 배경·스프라이트·영수 프레임을 런타임 좌표와 크기로 임시 합성해 배치와 통로를 시각 확인했다.

## Developer Final Check

> 개발자 소유 영역입니다. 실제 동작과 변경사항을 확인한 뒤 결과를 기록하고 상태를 `ACCEPTED`로 변경합니다.



## Completed Work Log

| Date | Task | Branch / PR / Commit | Result |
|---|---|---|---|
