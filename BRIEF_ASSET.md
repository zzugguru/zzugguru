# Current Asset Work Brief

이 파일은 에셋 작업 전용 요청, 질문, 결정과 결과를 공유하는 단일 창구입니다. 로직 작업의 `BRIEF.md`와 독립적으로 관리합니다.

## Status

`READY_FOR_DEVELOPER_CHECK`

상태 흐름: `DRAFT` → `READY` → `IN_PROGRESS` → `REVIEW` → `ACCEPTED`

## Developer Request

> 개발자 소유 영역입니다. 에이전트는 명시적인 요청 없이 수정하거나 삭제하지 않습니다.

### Task

Chapter 3 생체용기 에셋 및 상호작용 연출 고도화

### Goal

Chapter 3 기억 재구성 퍼즐의 임시 사각형 생체용기를 아내·큰아들·작은딸의 정체성이 드러나는 픽셀 아트 생체용기로 교체하고, 대기·연결·불안정·완료·개방 상태가 분명한 Canvas 연출을 적용한다.

### Context

- 관련 기능: `src/features/chapter3/client/MemoryReconstructionGame.ts`, `shared/gameLogic.ts`
- 현재 기억 재구성·각성 화면은 색이 다른 사각형과 텍스트만 표시해 생체용기라는 공간적 의미와 상태 변화가 약하다.
- 기존 가족 기억 연결 규칙과 `VESSEL_RECTS` 클릭 영역은 이미 동작하므로 그대로 보존한다.

### Included Scope

- 아내·큰아들·작은딸 각각의 인물 실루엣이 들어간 160×184 RGBA 단일 프레임 픽셀 아트 생체용기 PNG 생성
- 어두운 네이비 기계 프레임, 청록 유리 챔버, 케이블·생체신호 장치로 기존 우주선 연구실 화풍과 일치
- 퍼즐 화면의 대기·현재 연결·불안정·연결 완료 상태를 테두리·신호선·명시적 상태 문구로 구분
- 각성 화면에서 순차적으로 용기가 개방되는 상태를 시각화
- 이미지 로딩·native-size nearest-neighbor 렌더링과 기존 사각형 fallback 통합
- 래스터 메타데이터·알파 경계·draw geometry·상태 선택·fallback 테스트

### Excluded Scope

- `MEMORY_CLUES`, 가족별 요구 횟수, 정답·안정도·실패·각성 진행 규칙 변경
- `VESSEL_RECTS` 클릭·터치 판정 영역과 키보드 입력 변경
- 기존 가족 NPC, 외계 가족, 영수, 배경, 추억 오브젝트 에셋 변경
- 오디오·파티클 시스템·신규 디자인 토큰 추가

### Constraints

- `AGENTS.md`, `DESIGN.md`, `$two-agent-harness`, `imagegen` 절차를 따른다.
- 각 결과물은 160×184 8-bit RGBA PNG와 투명 배경을 사용하고 런타임에서 1:1 native size로 표시한다.
- 아내는 성인 여성, 큰아들은 12–14세, 작은딸은 7–9세이며 기억 속 설정에 맞는 평상복 실루엣을 유지한다.
- 이미지 자체에 글자·로고·UI를 넣지 않고 상태 정보는 Canvas의 기존 디자인 토큰 색과 한국어 문구를 함께 사용한다.
- `VESSEL_RECTS` 안에서 에셋·이름·진행 문구가 겹치지 않고 960×540 캔버스 경계를 넘지 않아야 한다.
- 로딩 실패나 잘못된 이미지 규격에서는 기존 사각형 표현과 텍스트를 유지한다.
- 모든 생성·수정·재검증 반복은 작업당 최대 2회이며, 이후 미완료 시 보고하고 진행 여부를 확인한다.

### Done When

- [ ] 세 가족 생체용기가 퍼즐·각성 화면에 1:1 픽셀 아트로 표시된다.
- [ ] 대기·현재 연결·불안정·연결 완료·개방 상태가 색뿐 아니라 문구와 형태 변화로 구분된다.
- [ ] 기존 클릭·터치 영역, 기억 연결 규칙, 안정도와 진행 흐름이 바뀌지 않는다.
- [ ] 이미지 실패 시 기존 사각형 fallback과 이름·진행 문구가 유지된다.
- [ ] 에셋 규격·알파·그리기 좌표·상태·fallback 회귀 테스트가 통과한다.
- [ ] 필수 검사와 Terra 독립 리뷰가 완료된다.

## Agent Understanding

> 메인 에이전트 소유 영역입니다. 구현 전에 이해한 목표, 범위와 완료 조건을 기록합니다.

`BRIEF_ASSET.md` 자산 lane에서 아내·큰아들·작은딸용 160×184 투명 픽셀 아트 생체용기를 생성하고 기억 재구성·각성 화면에 통합한다. 기존 연결 규칙과 `VESSEL_RECTS` 입력 계약은 보존하며, 대기·현재 연결·불안정·완료·개방 상태를 토큰 색과 명시적 문구로 함께 표현한다. 규격 오류와 로딩 실패 시에는 기존 사각형 fallback을 유지하고 래스터 메타데이터, native draw 좌표, 상태 선택과 fallback을 검증한다.

## Agent Questions

> 메인 에이전트 소유 영역입니다. 개발자의 판단이 필요한 질문과 확인 상태를 기록합니다.

없음. 2026-08-10 사용자가 생체용기 상호작용 연출의 구체적인 시각 방향과 구현 판단을 에이전트에게 맡기고, 이 요청으로 `Developer Request`를 교체해 진행하도록 승인했다.

## Developer Decisions

> 개발자 소유 영역입니다. 에이전트는 명시적인 요청 없이 수정하거나 삭제하지 않습니다.

- 2026-08-09: 사용자가 제공한 노년 경비원 캐릭터 이미지를 영수의 얼굴, 헤어스타일, 체형과 연령감의 주 참조로 사용한다. 손전등·무전기·커피·가족사진 소품은 스프라이트에 포함하지 않는다.
- 2026-08-09: 참고 이미지의 마른 노년 체형을 유지하기 위해 초기의 실루엣 약 26px 폭보다 시각적 일치를 우선한다. 최종 폭은 앞·뒤 18px, 좌·우 14px로 하되 960×540 맵 합성으로 가독성을 검증한다.
- 2026-08-09: 확정한 영수 스프라이트는 여러 챕터에서 사용할 공통 에셋으로 `src/assets/`에 보관한다.
- 2026-08-10: 원본 64×80 프레임은 유지하고 Chapter 3 게임 표시 크기를 48×60으로 확대한다. 충돌 영역은 기존 26×26을 유지한다.
- 2026-08-10: 재구성 가족과 작별한 뒤 누나와 대화하는 장면의 배경은 작고 소박한 우주선 내 영수 개인실로 설정한다.
- 2026-08-10: 영수 스프라이트는 Chapter 3에서 원본 프레임과 동일한 64×80 크기로 표시한다. 충돌·상호작용 크기는 26×26을 유지한다.
- 2026-08-10: Chapter 2 배경은 지하 경비실, 외계 어린 시절 방, 우주선 홀로그램 기록실, 우주선 전망실의 네 종류로 구성한다.
- 2026-08-10: 검증 에이전트는 `gpt-5.6-terra`로 고정하고, 모든 반복 작업은 최대 2회 후 사용자에게 계속 진행할지 확인한다.
- 2026-08-10: Chapter 2·3의 오브젝트 충돌 영역은 중심을 유지한 채 가로·세로를 90%로 축소한다.
- 2026-08-10: Chapter 2·3 상호작용 오브젝트는 배경과 같은 탑다운 픽셀 아트 에셋으로 교체하고, 작은 오브젝트는 투명 스프라이트 시트로 관리한다.
- 2026-08-10: 재구성 가족은 우주복·제복이 아닌 평상복을 입는다. 큰아들은 12–14세, 작은딸은 7–9세로 처음 생성안보다 어리게 표현한다.

## Agent Result

> 메인 에이전트 소유 영역입니다. 구현 결과, 검사 결과, 리뷰 대응과 남은 위험을 기록합니다.

- 내장 `imagegen`으로 기존 인간 가족 스프라이트와 우주선 연구실 화풍을 참조해 아내·큰아들·작은딸의 생체용기 3종을 생성했다. 평상복과 연령 설정을 유지하고 우주복·제복·텍스트·로고는 넣지 않았다.
- 크로마키 제거와 nearest-neighbor 정규화를 거쳐 세 에셋을 160×184 8-bit RGBA PNG로 저장했다. 모두 이진 알파 `{0,255}`를 사용하며 실제 알파 경계는 아내 `[43,4,117,180]`, 아들·딸 `[41,4,119,180]`이다.
- `bioVesselSprite.ts`에 에셋 경로, 1:1 native draw, 대기·수신·불안정·연결 완료·개방 상태 연출과 명시적 한국어 상태 문구를 구현했다. 퍼즐 규칙과 `VESSEL_RECTS` 입력 영역은 변경하지 않았다.
- 이미지가 미로딩·오규격이어도 기본 패널, 테두리, 가족 이름과 연결 진행이 유지된다. 퍼즐·각성 장면 모두 정보 카드는 `x=62–150`, 실제 불투명 용기는 최대 `x=157–235`에 배치돼 7px 이상 분리되고 패널 경계를 넘지 않는다.
- Terra 독립 리뷰가 최초 정보 바와 용기 알파의 겹침(P1), 소비 화면 fallback·장면 좌표 검사 부족(P2)을 발견했다. 용기를 오른쪽으로 이동하고 공통 패널 렌더러 및 실제 알파 기반 퍼즐·각성 좌표 테스트를 추가했으며, 같은 리뷰어의 재검증에서 두 건 모두 해결되고 새 차단 문제 없음이 확인됐다.
- 최종 검사: `npm run typecheck`, `npm run test`(62개 파일·288개 테스트), `npm run build`, `git diff --check` 모두 통과했다.
- 남은 위험: 제어 가능한 브라우저가 없어 실제 Canvas 화면의 스크린샷·클릭 조작은 확인하지 못했다. PNG 육안 검사, native draw 좌표, 실제 알파 경계, 상태 및 fallback 자동 검사로 대체했다.

## Developer Final Check

> 개발자 소유 영역입니다. 실제 동작과 변경사항을 확인한 뒤 결과를 기록하고 상태를 `ACCEPTED`로 변경합니다.



## Completed Work Log

| Date | Task | Branch / PR / Commit | Result |
|---|---|---|---|
